 1. What is Semantic HTML? writingand tagging HTML in a way that is readable to the computer and other developers
    2. What is HTML used for? It is the foundation for web development
    3. What is an attribute and where do we put it? HTML attributes provide additional information about HTML elements, we put them in  HTML sometimes witha Href <a> <img> style
    4. What is the h1 tag used for? How many times should I use it on a page?Should be only once for the most important headline on the page
    5. Name two tags that have required attributes <a> </a> Href-Name <img>
    6. What do we put in the head of our HTML document? Header our style sheet, links
    7. What is an id? It is a selector
    8. What elements can I add an id to? On any HTML element 
    9. How many times can I use the same id on a page? Once
    10. What is a class? An attribute which specifies one or more class names for an HTML
    11. What elements can I add a class to? All of them 
    12. How many times can I use the same class on a page? Multiple 
    13. How do I get my link to open in a new tab? Hold down the command key and click  the  link 
    14. What is the alt attribute used for? Alternate  text  for an image
    15. How do I reference an id? using the hash symbol #
    16. What is the difference between a section and a div Section is a part of the site where you can have several divs  inside, A div is just a divide in the page
    17. What is CSS used for? To add style to the website
    18. How to we select an element? Example - every h2 on the page h2{ 
                                                                    color: red;
                                                                    }
    19. What is the difference between a class and an id? - Give me an example of when I might use each one Class I can use over and over and style muptile things muptiple  elements can share the class. The ID will be used once on one specific element
    20. How do we select classes in CSS? Use a class selector
    21. How do we select a p element with a single class of “human””? .human  {
                                                                       color: red;
                                                                       }
    22. What is a parent child selector? When would this be useful? .human h1 {
                                                                     }
    23. How do you select all links within a div with the class of sidebar? div.sidebar {
                                                                             }
    24. What is a pseudo selector? Hover
    25. What do we use the change the spacing between lines? line heights?
    26. What do we use to change the spacing between letters? letter spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?Text transform property
    28. How do I add a 1px border around my div that is dotted and black? div {
                                                                           border: 1px dotted black;
                                                                           }
    29. How do I select everything on the page? hit edit and select all
    30. How do I write a comment in CSS? // *// //*
    31. How do I find out what file I am in, when I am using the command line? cd or ls
    32. Using the command line - how do I see a list of files/folders in my current folder?    ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm
    34. Why should I use version control? mistake  when working with a team 
    35. How often should I commit to github? often after making  any  changes  to my code
    36. What is the command we would use to push our repo up to github? the little 3 dots inside of vs code
    37. Walk me through Lambda's git flow. Fork
                                           add colab
                                           go  into file
                                           clone it
                                           create new branch
                                           then code
                                           make  sure its your own branch
                                           then code
                                           then  commit 
                                           then push
                                           then merge

Stretch Questions

    1. What is the difference between an inline element and a block element? inline blick is hard and block is a  little better
    2. What happens when an element is positioned absolutely? Its taking out the flow
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? Padding
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model? Box model gives you more control over ypour page and allows you to have more flexibility 

